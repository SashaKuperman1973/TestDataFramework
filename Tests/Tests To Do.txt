The randomizers for rest of the special collection types that are implemented in the old system.
Consider returning null when type recursion detected instead of throwing a TypeRecursionException. Maybe not. Remember to log it.

=Tests to write

=Scratchpad

* = curently working on
